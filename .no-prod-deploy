This repository has no production deployment. The global git pre-push guard
(~/.git-templates/hooks/pre-push) permits pushes to protected branches
(main/master/production/beta) when this committed marker exists at the repo root.
